package com.sec.imslogger.data

import android.content.ContentResolver
import android.content.ContentValues
import android.content.Context
import android.database.Cursor
import android.net.Uri

open class BaseMCSDBQuery(private val context: Context, private val contentUri: Uri) {

    private val contentResolver: ContentResolver = context.contentResolver

    // Function to query data from the content provider
    fun query(
        projection: Array<String>? = null,
        selection: String? = null,
        selectionArgs: Array<String>? = null,
        sortOrder: String? = null
    ): Cursor? {
        return contentResolver.query(contentUri, projection, selection, selectionArgs, sortOrder)
    }

    // Function to insert data into the content provider
    fun insert(values: ContentValues): Uri? {
        return contentResolver.insert(contentUri, values)
    }

    // Function to update data in the content provider
    fun update(values: ContentValues, selection: String?, selectionArgs: Array<String>?): Int {
        return contentResolver.update(contentUri, values, selection, selectionArgs) ?: 0
    }

    // Function to delete data from the content provider
    fun delete(selection: String?, selectionArgs: Array<String>?): Int {
        return contentResolver.delete(contentUri, selection, selectionArgs) ?: 0
    }
}
